# URL of the JogjaWaskita backend API
BACKEND_URL=http://localhost:8000

# Public-facing URL of this frontend (used for OAuth redirects in production)
# Not needed for local dev — only set in production/staging
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
